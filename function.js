exports.MaxVal = (a, b, c) => Math.max(a, b, c);
exports.PrintStrN_T = (str, num) => {
  for (let index = 0; index < num; index++) {
     console.log(str);
  }
};
exports.HowManyAfterP_Dig=(number)=>{
    
};
